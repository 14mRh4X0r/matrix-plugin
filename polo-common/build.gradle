plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

dependencies {
    implementation "org.slf4j:slf4j-api:${slf4jVersion}"

    // Minecraft provides this itself
    compileOnly 'com.google.code.gson:gson:2.7'
}
